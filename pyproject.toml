[project]
name = "aml-gnn-tesis"
version = "0.1.0"
description = "Proyecto de tesis para detección AML en Bitcoin con grafos y GNN."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = [
  "src"
]
